// keep constants neat
var keyMirror = require('keymirror');

module.exports = keyMirror({
  CREDENTIALS_AUTH: null,
  CREDENTIALS_AUTH_BACKEND: null,
  CREDENTIALS_AUTH_BACKEND_ERROR: null,
  CREDENTIALS_SIGNUP: null,
  CREDENTIALS_SIGNUP_BACKEND: null,
  CREDENTIALS_SIGNUP_BACKEND_ERROR: null,
});
